# Инструкция по переключению на продакшен (версия для игроков)

## Что нужно сделать

Открыть файл `core/constants.js` и изменить две строки:

```javascript
// Было (DEV режим):
window.BETA_MODE = true;
window.DEV_MODE = true;

// Стало (PRODUCTION режим):
window.BETA_MODE = false;
window.DEV_MODE = false;
```

## Что изменится

### ❌ Будет отключено:

1. **DEV-кнопки быстрого завершения**
   - В модалке строительства/изучения НЕ будет кнопки "🚀 [DEV] Завершить мгновенно"
   - Файл: `city/time-construction-system.js`

2. **Подсветка кликабельных зон**
   - В библиотеке зоны НЕ подсвечиваются при наведении
   - На карте города полигоны зданий НЕ видны (желтые контуры)
   - Файлы: `spells/library_ui.js`, `city/city-clickable-system.js`

3. **Ускорение времени**
   - Строительство/изучение вернется к нормальной скорости (1x вместо 10x)
   - Пример: библиотека будет строиться 1440 минут (24 часа) вместо 144 минут (2.4 часа)

### ✅ Останется работать:

- Все игровые функции
- Нормальное ускорение через валюту времени
- Все анимации и визуальные эффекты
- База данных и сохранения
- Вся игровая логика

## Как вернуть DEV режим

Просто верните значения обратно на `true`:

```javascript
window.BETA_MODE = true;
window.DEV_MODE = true;
```

## Проверка

После изменения:
1. Перезагрузите страницу
2. Откройте библиотеку - зоны не должны подсвечиваться
3. Начните изучение заклинания - НЕ должно быть DEV-кнопки
4. Время строительства должно быть полным (без 10x ускорения)

---

**ВАЖНО:** Изменять нужно только `core/constants.js`, остальные файлы трогать не нужно!
